[package]
name = "route-review"
version = "0.1.0"
description = "Route Review desktop wrapper"
authors = ["Route Review contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
